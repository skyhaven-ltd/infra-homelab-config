apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: jellyseerr
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
  - reconcile-job.yaml
configMapGenerator:
  - name: jellyseerr-scripts
    files:
      - reconcile-services.py
