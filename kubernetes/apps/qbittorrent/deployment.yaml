apiVersion: apps/v1
kind: Deployment
metadata:
  name: qbittorrent
  namespace: qbittorrent
spec:
  replicas: 1
  strategy:
    type: Recreate # RWO config volume
  selector:
    matchLabels:
      app: qbittorrent
  template:
    metadata:
      labels:
        app: qbittorrent
    spec:
      containers:
        - name: qbittorrent
          # Pinned by digest = old container's exact running :latest image (§1.11).
          image: lscr.io/linuxserver/qbittorrent@sha256:5b09709bb0eff4edb551f5b30029952ab4d67aa0d5ca3526889124173bd78a9c
          env:
            - { name: PUID, value: "1000" }
            - { name: PGID, value: "1000" }
            - { name: TZ, value: "Etc/UTC" }
            - { name: WEBUI_PORT, value: "8080" }
          ports:
            - { name: http, containerPort: 8080 }
            - { name: bt-tcp, containerPort: 6881, protocol: TCP }
            - { name: bt-udp, containerPort: 6881, protocol: UDP }
          volumeMounts:
            - { name: config, mountPath: /config }
            - { name: media, mountPath: /data }
          resources:
            requests: { cpu: 100m, memory: 128Mi }
            limits: { memory: 512Mi }
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: qbittorrent-config
        - name: media
          hostPath: { path: /mnt/media, type: Directory }
