apiVersion: apps/v1
kind: Deployment
metadata:
  name: bookbuddy
  namespace: bookbuddy
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: bookbuddy
  template:
    metadata:
      labels:
        app: bookbuddy
    spec:
      containers:
        - name: app
          image: ghcr.io/skyhaven-ltd/infra-homelab-config/bookbuddy-web@sha256:30d2394c1afed88d09527b19b70d82cc78136434a7a53c708c8b6c7df5d7c210
          # TLS terminates at the ingress; without proxy headers uvicorn sees
          # http and the app generates mixed-content absolute URLs.
          command: ["uvicorn"]
          args:
            - app.main:app
            - --host
            - "0.0.0.0"
            - --port
            - "8080"
            - --proxy-headers
            - --forwarded-allow-ips
            - "*"
          env:
            - { name: DATABASE_URL, value: "sqlite:////data/bookbuddy.db" }
            - { name: UPLOAD_DIR, value: "/data/uploads" }
          envFrom:
            # Optional so the app runs before the secret is sealed; worker
            # endpoints return 503 until WORKER_TOKEN exists.
            - secretRef:
                name: bookbuddy-env
                optional: true
          ports:
            - { name: http, containerPort: 8080 }
          volumeMounts:
            - { name: bookbuddy-data, mountPath: /data }
          livenessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            httpGet: { path: /health, port: 8080 }
            initialDelaySeconds: 10
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          resources:
            requests: { cpu: 50m, memory: 128Mi }
            limits: { memory: 512Mi }
      volumes:
        - name: bookbuddy-data
          persistentVolumeClaim:
            claimName: bookbuddy-data
