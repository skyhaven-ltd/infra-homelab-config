apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: recyclarr
resources:
  - cronjob.yaml
  - sync-job.yaml
configMapGenerator:
  - name: recyclarr-config
    files:
      - recyclarr.yml
