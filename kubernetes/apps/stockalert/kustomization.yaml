apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: stockalert
resources:
  - pvc.yaml
  - deployment.yaml
  - service.yaml
  - ingress.yaml
configMapGenerator:
  - name: stockalert-config
    files:
      - config.yaml
