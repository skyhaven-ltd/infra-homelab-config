apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-config
  namespace: prowlarr
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      # Old /config ~88M; nominal headroom.
      storage: 1Gi
