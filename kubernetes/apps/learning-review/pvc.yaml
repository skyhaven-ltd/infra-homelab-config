apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: learning-data
  namespace: learning-review
spec:
  accessModes: [ReadWriteOnce]
  resources:
    requests:
      # ~1.5M seeded today (learning.db). local-path does not enforce capacity;
      # nominal headroom.
      storage: 2Gi
