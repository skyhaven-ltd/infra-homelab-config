apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: backlog-portal
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/skyhaven-ltd/infra-homelab-config.git
    targetRevision: main
    path: kubernetes/apps/backlog-portal
  destination:
    server: https://kubernetes.default.svc
    namespace: backlog-portal
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
