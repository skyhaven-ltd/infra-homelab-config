name: "Break Terraform State Lease"
description: "Breaks an Azure Storage blob lease on the Terraform state file left by a failed run"

inputs:
  storage_account_name:
    description: "Name of the Azure Storage account"
    required: true
  container_name:
    description: "Name of the blob container holding the state file"
    required: true
  state_key:
    description: "Blob name of the state file"
    required: false
    default: terraform.tfstate
  azure_client_id:
    description: "Azure client ID for OIDC login"
    required: true
  azure_tenant_id:
    description: "Azure tenant ID for OIDC login"
    required: true
  azure_subscription_id:
    description: "Azure subscription ID for OIDC login"
    required: true

runs:
  using: composite
  steps:
    - name: "Azure Login"
      uses: azure/login@a457da9ea143d694b1b9c7c869ebb04ebe844ef5 # v2
      with:
        client-id: ${{ inputs.azure_client_id }}
        tenant-id: ${{ inputs.azure_tenant_id }}
        subscription-id: ${{ inputs.azure_subscription_id }}

    - name: "Break State Lease"
      shell: bash
      env:
        STORAGE_ACCOUNT_NAME: ${{ inputs.storage_account_name }}
        CONTAINER_NAME: ${{ inputs.container_name }}
        STATE_KEY: ${{ inputs.state_key }}
      run: |
        LEASE_STATE=$(az storage blob show \
          --account-name "$STORAGE_ACCOUNT_NAME" \
          --container-name "$CONTAINER_NAME" \
          --name "$STATE_KEY" \
          --auth-mode login \
          --query "properties.lease.state" \
          --output tsv 2>/dev/null || echo "missing")

        if [[ "$LEASE_STATE" == "leased" ]]; then
          echo "Active lease found — breaking..."
          az storage blob lease break \
            --account-name "$STORAGE_ACCOUNT_NAME" \
            --container-name "$CONTAINER_NAME" \
            --blob-name "$STATE_KEY" \
            --auth-mode login \
            --output none
          echo "Lease broken."
        else
          echo "No active lease (state: ${LEASE_STATE}). Nothing to do."
        fi
