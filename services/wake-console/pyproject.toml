[project]
name = "skyhaven-wake-console"
version = "0.1.0"
description = "A fixed-target Wake-on-WLAN web console."
requires-python = ">=3.13,<3.14"

[tool.ruff]
line-length = 100
target-version = "py313"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM", "RUF"]

